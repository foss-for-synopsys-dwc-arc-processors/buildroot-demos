#!/bin/sh
# start stop hotspot

start() {
        export DISPLAY=:0
        glmark2 &
}

stop() {
        echo "Killing glxgears"
        killall glmark2
        echo "OK"
}

case "$1" in
  start)
	start
	;;
  stop)
	stop
	;;
  *)
	echo "Usage: $0 {start|stop}"
	exit 1

esac
exit $?
