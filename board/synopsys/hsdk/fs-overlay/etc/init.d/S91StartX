#/bin/sh

case "$1" in
  start)
    echo "Starting Xserver"
    X &
    ;;
  stop)
    echo -n "Killing Xserver"
    killall X
    ;;
  restart|reload)
    "$0" stop
    "$0" start
    ;;
  *)
    echo $"Usage: $0 {start|stop|restart}"
    exit 1
esac

exit $?
